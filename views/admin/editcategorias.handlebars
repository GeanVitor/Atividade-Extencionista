{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}
<h3>Editar categoria</h3>
<div class="card bg-dark">
    <div class="card-body">
        <form action="/admin/categorias/edit" method="POST">
            <input type="hidden" name="id" value="{{categoria._id}}" style="color: white;">
            <label for="nome" style="color: white;">Nome:</label>
                <input type="text" id="slug" name="slug" placeholder="slug da categoria" class= "form-control bg-dark" style="color: white;" value="{{categoria.slug}}">

            <label for="slug" style="color: white;">slug:</label>
                <input type="text" id="slug" name="slug" placeholder="slug da categoria" class= "form-control bg-dark" style="color: white;" value="{{categoria.slug}}">
                <br>
        <button type="submit" class="btn btn-success mt-3">Salvar</button>
    </div>
</form>
</div>