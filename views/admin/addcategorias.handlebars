{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}
<h3>Nova categoria</h3>
<div class="card bg-black" style="color: white;">
    <div class="card-body bg-black">
        <form action="/admin/categorias/nova" method="POST">
    <label for="nome" style="color: white;">Nome:</label>
        <input type="text" id="nome" name="nome" placeholder="Nome da categorias" class="form-control bg-dark" style="color: white;">
    <label for="slug" style="color: white;">slug:</label>
        <input type="text" id="slug" name="slug" placeholder="slug da categoria" class="form-control bg-dark" style="color: white;">
        <br>
    <button type="submit" class="btn btn-success">Criar categoria</button>
    </div>
</form>
</div>